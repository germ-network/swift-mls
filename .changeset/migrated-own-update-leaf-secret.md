---
"@germ-network/swift-mls": patch
---

The migration-only own-Update insert (`@_spi(Migration) Group.insertMigratedOwnUpdate`) can now take the Update's leaf secret directly instead of requiring it in the group state.
